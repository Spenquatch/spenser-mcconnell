import type { NextConfig } from 'next';

const nextConfig: NextConfig = {

  // Add any other existing config here
  experimental: {
    // Add experimental features if you have any
  },
  
  // Other common config options
  images: {
    // Configure if you have image optimization settings
  },
};

export default nextConfig;